# 2026-09-03

## 预算止损工作包收尾 + 提交（657939f）

- 接续 09-02 深夜未提交的工作包（上游额度耗尽止损 + simple 任务短路 + 默认权限 build），核查发现缺测试覆盖，补齐 6 个用例：
  - orchestrator.test.mjs：止损闸（未确认续聊拒 BUDGET_EXHAUSTED 且挡在 provider 调用前；确认后放行并清 failureKind/error）+ simple 短路（单轮主脑、`simpleShortCircuit: true`、零委派边）。fixture 加 `route` 覆盖参数。
  - adapters.test.mjs：claude-cli 上游额度分类——result 路径（errors 含「额度不足」）、非 result 路径（stderr 403 insufficient credits）+ 负例。`ClaudeCliAdapter` 可注入 `runProcessImpl`，stdout 喂 LF-JSON 事件即可模拟 terminal result。
- 验证：orchestrator 127、adapters 73、router 34、remote-run 全绿；qa:ui --suite=layout ok；qa:delivery clean/drift=0。
- 提交 `657939f`（9 文件，显式 pathspec）。

## EQ-03 qa:ui 套件：4/5 修绿 + 一个真实产品缺陷（50f0e8b）

- `--suite=all` 逐套件隔离浏览器诊断（一次性 bootstrap：**每次 qa:ui.mjs 直跑都要新 fixture 服务**，qa-ui-fixture.mjs 会自动起新的）。
- 修复 5 项漂移，全部有现场证据：
  1. **mobile mission 卡死**：≤560px 顶栏 `#global-mc-toggle` 被有意 display:none（console-form.css:1419），脚本静默跳过重开 → dock 停留折叠。修：`expandMissionDock` 幂等助手，回退派发应用自身 `forge:expand-mission-control` 事件（workbench-chrome.js:217 监听）。
  2. **delta seed 不渲染**：会话流按成员分页（conversation-tabs），默认页=默认收件人=主脑；mock 事件挂 codex-technical 被过滤。修：seed/delta/边界流事件 agentId 改挂 claude-fable。
  3. **rail 点击视口外**：390px 左栏是抽屉，locator.click 永远重试。修：程序化 click（mission 套件同款先例）。
  4. **项目树竞速**：内核 CLI-env 初始化晚于 UI 首轮扫描 → 「项目扫描不可用」。修：空树时周期点 `#refresh-button` 重扫（workbench 视图会 loadProjects refresh）。
  5. **sr-only 开关 check() 失败**：rail-toggle input 视觉隐藏，中心命中被 span 拦截。修：点 label。
- **真实产品缺陷（顺带发现并修）**：Mission Control 右栏是脱离 grid 的浮层抽屉，展开时 composer 右缘（发送/新任务/存为自动化）被盖住不可点——elementFromPoint 实证。修：codex-desktop.css 给 `.task-composer` 加同终端抽屉的让位规则（≥821px + :not(.mc-collapsed)）。残留：会话流右缘 action/运行头右侧控件待 UX-04 复查。
- **workbench 状态机套件未修完（诚实记录）**：停在 qa-ui.mjs:877 拓扑键控。根因是测试对着旧 UI 模型写（qa-ui.mjs 自 5a2eb57 未更新）：成员页模型下 `openTab` 总解析具体收件人、无独立团队页 tab；Enter 激活成员页后 `#session-topology` 重渲 `renderTopology(null)`="暂无会话"，第二按钮不复存在——「Enter 后原地 Space」「tab≥3」断言不可能成立。**需半天级测试重设计，非单点修复**（主计划 B-05 节）。
- 提交 `50f0e8b`；ui:lint 全绿；workbench-rail/mission-control 合同 31/31。

## EQ-02 Bot P0 闭环

- `qa:bot-p0` 自带 fixture（spawnTestServer + 隔离浏览器），直接跑：exit=0，结果 JSON 0 项 false。会话恢复/短 id 搜索/键盘入口/历史装载/消息准入 202/mobile 溢出 0/inspector 四视口稳定。

## 主计划与台账

- v46 主计划：状态头四次复测、15.1 EQ-02 闭环、新增 15.5 四次复测台账、B-03 解锁链更新、新增 B-05（workbench 套件语义漂移）、17 节拍板项更新。
- 剩余 Wave 0 项：EQ-01 full 基线连续 5 次、EQ-03 workbench 套件重设计、SG-01 token 轮换（LO 手动）、formal 授权。

## 环境坑（新增）

- qa-ui.mjs 直跑（绕过 qa-ui-fixture）时 URL 的 bootstrap token 是**一次性**的：被任何浏览器消费后 badge 等待必超时。诊断工作流 = 每轮诊断重起一个 manual-fixture-server（.scratch/manual-fixture-server.mjs）。
- 内核 CLI-env 初始化会向 `$HOME/.claude/projects/<cwd-slug>/` 写会话文件（fixture 重定向 HOME 后写进 runtimeHome），项目树扫描结果依赖启动时序。

## push 授权执行 + B-05 深挖（追加）

- LO 授权后 3 提交（657939f/50f0e8b/db63806）经 PowerShell push 成功，remote main = db63806；本地跟踪引用按既有坑用 `echo <sha> > .git/refs/remotes/origin/main` 修复，`git ls-remote` 验证。
- B-05 深挖两个新实证（已补进主计划）：
  1. 拓扑成员卡 Enter 激活会**切出当前 run 上下文**（标题变「会话 <shortId>」、拓扑重渲 null）——「Enter 后原地 Space」旧序列结构性失效，准确契约需产品拍板后再写断言；
  2. state machine 套件 GET /api/runs 未 mock，内核占位会话行混入 rail——重设计时必须连 GET 一起 mock（mission 套件同法）。
- close-tab 级联在 <3 tab 优雅跳过，不会连坏。
- 结论维持：workbench 套件是半天级重设计，别再单点修补。

## EQ-01 全量基线启动（追加）

- 数据点 #1：`npm test` 1995 tests / 1993 pass / **0 fail** / 2 skipped，clean-exit 全 ok，exit 0。B-02 修复确认收敛。
- 第 2-5 次链式后台执行中（`for i in 2 3 4 5; do npm test > .scratch/eq01-run$i.log`），5 次全绿即 EQ-01 闭环。

## EQ-01 闭环（最终）

- 连续 5 次全量基线全绿：每次 1995 tests / 1993 pass / 0 fail / 2 skipped，clean-exit 4 门全 ok，无 flaky。
- Wave 0 状态：EQ-01 ✅ / EQ-02 ✅ / EQ-03 四套件绿（workbench 重设计挂 B-05）/ drift=0 / 剩 SG-01（LO 手动）+ formal 授权。
- Wave B 切片可在「每刀 qa:ui --suite=layout 冒烟」纪律下恢复（主计划拍板项 4 已更新）。
- 拓扑激活契约已列为拍板项 9（A 维持跳转独立会话 / B 改为 run 内成员页 tab）。

## B-05 重设计第一批完成（追加）

- 状态机套件全绿：拓扑键控改「聚焦校验+效果断言+重试」（codex 卡 Space 建 tab、claude 卡 Enter 切回）；重开入口迁拓扑卡；成员条切换迁收件人 radio（selectComposerTarget→openTab）；18 处 mock 事件归挂 claude-fable；pageerror 立即 stderr 输出（20 处）。
- collapsed-project 树检查确认结构性失效：项目树换代为「当前团队平铺列表+未归属组」，data-team-toggle 团队节点已不存在——实测排除 teams 竞速（API 命中 4 次仍不渲染）。需同批重设计。
- 长任务预算（>50ms）在 53-69ms 间环境敏感抖动，建议放宽或 P95 统计。
- 教训：bash→python→文件三层反斜杠转义极易翻车，行级修改用 chr(92) 显式构造。
- remote-projects mock 补 4 处（loadProjects 渲染前 await 真实探针，测试隔离缺陷）。

## 收官状态（追加）

- 最终全套件：mission/delta/state-machine/sse-dom/history-cache/abort-reopen 全绿；仅剩 collapsed-project 失效等待点（结构性，下一批转写为未归属组惰性挂载模型）+ 长任务预算环境敏感抖动。
- 6 个提交已推送（远端 main = 3c1392d）。
