# 2026-09-02

## 接续 Qoder 任务 f8aeca53：进度核查 + B-04 前端启动回归定位修复

- Qoder 任务（Wave B app.js 解耦）停在 slice 22 半途：`conversation-messages.js`（350 行）已抽取未提交，最后一动作是跑全量测试时上下文耗尽。
- **定位并修复 LO 报障「正在加载团队与项目…卡死」的代码级根因（B-04）**：两处 TDZ——
  1. `app.js` `createBotSettlement`（~16847）DI 传 `normalizeRunMessages`，早于 slice 22 工厂 const 解构（~22866）；
  2. `app.js` `createWorkbenchTopology`（~23115）DI 传 `eventTracksEvent`，早于 slice 19 `deltaMerge` const 解构（~24402）。
  均改为惰性转发箭头函数。隔离浏览器验证 `BADGE OK / API 已连接`；`qa:ui --suite=layout` 跑通；受影响测试 55/55。
- 顺带修正 03:20 轮复测的错误结论「应用本体没有被 Wave B 切片打断」——那只验了服务端 /readyz，没加载前端。
- 另一阻塞 B-01（实例锁僵持，pid 41872 只持锁不服务 HTTP）当日已自愈；`.scratch/control-center-fatal.log` 记录 20×INSTANCE_ACTIVE + 1×EADDRINUSE 8765，是桌面端启动失败的服务端证据。
- `proposals/v46-collaboration-workbench-master-plan.md` 已补 15-18 节执行台账 + B-04 修正 + 解锁顺序（本轮更新到 04:00）。
- 数字基线：modules=58（物理）、app.js=28,694 行、validate 13/13、ui:lint 全过（inner-html 387/389）、delivery drift 18→1（仅剩未提交 slice 22）、origin/main 领先 15 提交。
- **未做**（按项目确认门纪律）：commit slice 22 + TDZ 修复（pathspec 五文件：app.js、conversation-messages.js、conversation-messages-module.test.mjs、bot-shell-ui.test.mjs、codex-process-visibility.test.mjs）、push、清理 823 个 `.test-*` 残留目录（需 LO 授权）、B-02 干净环境全量基线。
- 补充：`tests/conversation-messages-module.test.mjs`（slice 22 专属测试）4/4 通过；04:08 轮全量测试完整跑完——失败主体仍是 B-02 safe-delete 拦截伪影，另有 `clean-exit:resource=fail`（疑似句柄泄漏，需单独归因）。

## 归因并修复全量测试唯一真实失败：ccswitch-proxy.test.mjs:1215

- 之前把 `ccswitch-proxy.test.mjs:629` 当唯一真实失败，实为漂移抖动（整文件单跑 36/37 时该用例通过）。真正的稳定失败是 `:1215`「close 插入 proxy 配置发布前时，updateConfig 的内存与磁盘都恢复旧快照」，4/4 稳定失败 `121000 !== 120000`。
- 根因链（`src/ccswitch/proxy.mjs` `updateConfig` catch）：
  1. 回滚写 `#writeConfigSync(previous, { deadline: () => this.#lifecycleDeadline(lifecycle) })` 复用了 close 的 deadline（测试里 `shutdownTimeoutMs:80`，解析为 `signal.reason.deadline`）。
  2. `#commit` 的 catch 里 `rmSync(temp)` 在 Windows 上被实时杀毒扫描卡了 ~500ms（探针实测 532ms），把 80ms deadline 耗光。
  3. 回滚写抛 `PROXY_CONFIG_PERSIST_TIMEOUT` → 旧实现 `this.config = publishedConfig` 把内存错误覆盖回新值（而磁盘其实仍是旧快照，因为 `#commit` 的 rename 在 abort 前根本没执行）。
- **修复**：回滚写去掉 deadline（`#writeConfigSync(previous)`，同步且有界，不会挂死）；移除错误的 `this.config = publishedConfig` + `configureLocalProxy(publishedConfig)` 回退，回滚写失败只记录 `cancellation.rollbackError`；删除死变量 `publishedConfig`。
- 验证：`ccswitch-proxy.test.mjs` 整文件 37/37 通过。
- v46 主计划 B-02 节、解锁顺序、状态头已更新为「唯一真实失败 1215 已修复」。
- 未提交状态不变：slice 22 + 两处 TDZ（B-04）+ 本修复（proxy.mjs）仍在工作区，需 LO 授权后按显式 pathspec 提交。

## 桌面端无法启动 — 已修复（实例锁僵持）
- **现象**：LO 反馈桌面端仍无法启动；fatal.log 反复 `INSTANCE_ACTIVE`，desktop-boot.log 只有 spawn 无 `kernel URL ready`。
- **根因**：`.ai-shared/control-center/control-center.lock` 属主 pid 11332 是健康、正在服务（127.0.0.1:59001，readyz 200）的旧实例，自 09-01 19:32Z 残留，不在 desktop-boot.log 孤儿清理轨迹（只清过 pid 14024）。桌面端每次 spawn 的新 kernel 撞锁 → `instance-lock.mjs:89` 抛 INSTANCE_ACTIVE → 退出。
- **处置（LO 授权「终止并清锁」）**：`taskkill /PID 11332 /T /F`（注：11332 是父进程 27952 的子进程，父进程已随树消失）→ `rm control-center.lock`。
- **验证**：拉起 release 版 `cc-desktop.exe` → 新 kernel pid 28692 抢锁成功、监听 51400、`kernel URL ready: http://127.0.0.1:51400/#bootstrap=...`、`/readyz` 全绿（event-store/orchestrator/health-service 均 ready），fatal.log 无新 INSTANCE_ACTIVE。
- **长期缺口（列入 backlog）**：`lockOwnerIsActive` 只查 pid 存活 + 镜像名，无 HTTP 活性证明，无法区分「健康旧实例」与「真孤儿」；桌面端 Rust 壳只有 spawn 无「检测到健康同源实例则复用」路径。建议后续补 HTTP 活性证明 + 复用逻辑（B-01 残余缺口）。

## slice 22 + proxy.mjs + 计划文档 提交完成
- **slice 22 验证**：conversation-messages 模块抽取，接线干净（ESM import + 工厂闭包依赖注入，与 slice 19/21 同模式）；函数归属正确；B-04 两处 TDZ 修复确在 app.js diff 内（normalizeRunMessages / eventTracksEvent 惰性转发，调用点前移触发）。测试：新模块 4/4 绿 + bot-shell-ui 43 + codex-process-visibility 12 = 55/55 绿，clean-exit 全 ok。
- **提交（LO 授权「全部提交」）**：
  - `8a36346` refactor: slice 22 — conversation-messages 抽取（5 文件 +455/-330）
  - `169879c` fix: proxy updateConfig 回滚写复用 deadline 缺陷（1 文件 +4/-7）
  - `21366d3` docs: v46 主计划 B-02/解锁顺序/状态（1 文件 +200/-3）
- 工作区已清零，main ahead 18（未推送）。
- **待办**：push 18 提交（需授权）；清理 `.test-*` 残留（约 1772 个，官方入口 `node scripts/run-tests.mjs --clean-only`，需授权）；SG-01 token 轮换（LO 手动）。

## push 与残留清理结果（追加）
- **.test-* 残留**：现已 0 个（全仓复核）。盘点时 1772 → 经几轮测试 clean-exit 自清（conversation-messages 测试 swept 210）+ `--clean-only` 后归零，目标达成。
- **push 受阻**：remote = https://github.com/lanniny/514-bot.git，credential.helper = helper-selector（GCM）。https push 需 GCM 交互认证 → 环境内 SIGTERM；ssh 无 github 配置且 `ssh -T git@github.com` 被透明代理（198.18.0.157 保留地址）拦截。结论：push 需 LO 手动处理认证（SG-01 token 轮换 / 手动 push / 提供 PAT）。main 仍 ahead 18。

## push 最终成功（曲折过程 + 环境坑，纠正上节「受阻」结论）
- **结论**：18 个提交已全部推到 remote，本地 main 与 origin/main 完全同步（`ahead/behind = 0 0`），remote main = 21366d3（`git ls-remote origin` 确认）。
- **过程**：Bash 里 `git push` × 3 次均 SIGTERM（GCM helper-selector 认证交互在 Bash 环境卡死）；换 Windows 原生 shell 跑 `git push` 返回 exit 0 空输出，**实际 push 成功**（remote 已更新），但该 shell 环境里 git 对 `refs/remotes/*` 的原子写入（lock+rename）异常 → 本地 `origin/main` 跟踪引用未更新反而消失（`git status` 显示 `[gone]`）。
- **修复**：`git ls-remote origin` 确认 remote 真实状态 → `git fetch` / `git update-ref` 均「声称成功但文件不落盘」→ 最终用 shell 直接 `echo <sha> > .git/refs/remotes/origin/main` 写入成功，git 立即识别。
- **环境坑（重要，会反复遇到）**：此 Windows 环境里 git 对 `refs/remotes/*` 的原子写入失效，但直接写引用文件有效。验证 push 是否成功唯一可靠方式 = `git ls-remote origin`（看 remote 真实 HEAD）。
