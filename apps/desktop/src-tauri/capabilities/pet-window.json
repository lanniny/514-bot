{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet-window",
  "description": "514 pet overlay window: drag handle and click-through toggle only. The pet page never calls native bridge commands.",
  "local": false,
  "windows": [
    "pet-overlay"
  ],
  "remote": {
    "urls": []
  },
  "permissions": [
    "core:window:allow-start-dragging",
    "core:window:allow-set-ignore-cursor-events"
  ]
}
