#!/usr/bin/env node
// B1 command compatibility: the canonical workspace now owns its acceptance
// suite and isolated preview fixture. CLI output-dir/serve options are retained.
await import("./qa-collaboration-fusion.mjs");
